flask>=3.0.0
requests>=2.31.0
python-dotenv>=1.0.0
flask-caching>=2.1.0
anthropic>=0.40.0
gunicorn>=21.2.0
# Khipu Finance v1 — API pública (JWT) y métricas de riesgo (VaR/CVaR)
pyjwt>=2.8.0
numpy>=1.26.0
pytest>=7.4.0
# Grafo de Conocimiento Temporal — persistencia opcional en Neo4j (driver oficial,
# ligero). Los hechos ya vienen estructurados, así que no se necesita Graphiti
# ni un proveedor de embeddings. Se activa con NEO4J_URI/USER/PASSWORD.
neo4j>=5.14.0
